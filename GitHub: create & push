# From your machine, after generating this repo locally
# 1) Initialize git
git init


# 2) Add files
git add .


# 3) First commit
git commit -m "alpha-engine: MVP scaffold"


# 4) Create a repo on GitHub
# - Go to https://github.com/new
# - Name: alpha-engine (or your choice)
# - Set visibility (public if you want open-source)
# - Do NOT initialize with README (we already have one)


# 5) Add remote (replace USER with your username)
git remote add origin git@github.com:USER/alpha-engine.git


# 6) Push
git branch -M main
git push -u origin main